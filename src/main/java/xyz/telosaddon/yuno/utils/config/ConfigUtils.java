package xyz.telosaddon.yuno.utils.config;

//import io.wispforest.owo.config.Option;

import java.util.function.Consumer;

import static xyz.telosaddon.yuno.TelosAddon.CONFIG;

public class ConfigUtils {

//    public static <T> void addConfigBinding (Option.Key configKey, Consumer<T> onChanged) {
//        System.out.println("Registering config binding for " + configKey);
//        Option<T> option = CONFIG.optionForKey(configKey);
//        if (option != null) {
//            option.observe(onChanged);
//        }
//    }
}
